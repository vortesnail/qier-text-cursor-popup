function fuck(num, str): void {
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
  console.log(`不着好${num}`);
}

const arrow = (num) => {
  return num;
};

fuck(1, 'ss');
